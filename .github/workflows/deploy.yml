name: Deploy Spring Boot to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Make Maven Wrapper Executable
        run: chmod +x mvnw

      - name: Build JAR
        run: ./mvnw clean package -DskipTests

      - name: Copy JAR to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          source: "target/productservice-0.0.1-SNAPSHOT.jar"
          target: "/home/ubuntu/"
          strip_components: 1        # ← This copies JAR directly to /home/ubuntu/ not /home/ubuntu/target/

      - name: Restart Application on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          script: |
            cd /home/ubuntu
            rm -f app.log

            # Kill old process safely without triggering Docker KILL
            OLD_PID=$(pgrep -f productservice-0.0.1-SNAPSHOT.jar)
            if [ -n "$OLD_PID" ]; then
              kill $OLD_PID 2>/dev/null
              sleep 3
            fi

            nohup java -jar productservice-0.0.1-SNAPSHOT.jar > app.log 2>&1 &